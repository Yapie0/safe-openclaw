// Narrow plugin-sdk surface for the bundled security-shield plugin.
// Keep this list additive and scoped to symbols used under extensions/security-shield.

export { emptyPluginConfigSchema } from "../plugins/config-schema.js";
export type { OpenClawPluginApi } from "../plugins/types.js";
